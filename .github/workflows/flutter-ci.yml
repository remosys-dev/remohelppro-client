name: Full Flutter CI

on:
  # REMOHELP PRO: 自動起動(push/PR)を無効化。手動実行のみ（狙ったビルドだけ回す・分数節約）。
  workflow_dispatch:

jobs:
  run-ci:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: false
