/// 当社サーバーの場所。**ここ1か所だけ**に書く。
///
/// 🔴🔴 なぜ集めたか（2026-09-04 社長のご判断・案A の第1段）
///
///   ⚠ それまで `https://svr.remohelppro.jp` が**9か所に散らばって**いた。
///     ・`_kApiBase` という同じ名前の定数が**4つのファイルに重複**
///     ・ほかに直書きが3か所（戻る窓・終了・ログオン前の確認）
///     ・お客様への案内文に1か所
///   ⚠ 散らばっていると、⚠ **1か所直しても残りが本番を見る**。
///     同じ形の事故（同じ壁が入口ごとに3か所）を、今日だけで3回起こしている。
///
/// 🔴 これから何をしたいか
///   試験用の版（`dev.remohelppro.jp` を見る版）を出せるようにする。
///   ⚠ 散らばったままでは、⚠ **切り替え忘れが必ず出る**。
///   ★先に1か所へ集める。切り替えを足すのは、その次。
///
/// ⚠ **この段では動きを変えない。** 値は本番のまま。
///   切り替えを足すときも、⚠ **既定は必ず本番**にすること。
///   切り替え忘れの版が試験用を向くと、⚠ **お客様が繋がらなくなる。**
library;

/// 当社サーバー（API・顧客ページ）の場所。`https://` を含む。
///
/// ⚠ 末尾に `/` を付けないこと。呼び出し側が `'$kRlApiBase/api/...'` と書く。
const String kRlApiBase = 'https://svr.remohelppro.jp';

/// 画面や案内文に出す「ホスト名だけ」の形（`https://` を外したもの）。
///
/// ⚠ お客様に読み上げていただく文で使う。⚠ **別に書かないこと。**
///   別に書くと、切り替えたときに⚠ **案内文だけ本番のまま**になる。
const String kRlApiHost = 'svr.remohelppro.jp';
